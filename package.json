{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "prisma migrate deploy && prisma generate && node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "fmt": "prettier --write '**/*.js'"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "@slack/bolt": "^4.6.0",
    "chrono-node": "^2.9.0",
    "express": "^5.2.1",
    "slack-block-builder": "^2.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prettier": "^3.8.1",
    "prisma": "^5.16.1",
    "puppeteer": "^24.36.0",
    "typescript": "^5.9.3"
  }
}
